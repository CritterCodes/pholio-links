{
  "name": "pholio-domain-setup-server",
  "version": "1.0.0",
  "description": "Server API for programmatically setting up custom domains with nginx and certbot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "nginx",
    "certbot",
    "custom-domains",
    "ssl"
  ]
}
